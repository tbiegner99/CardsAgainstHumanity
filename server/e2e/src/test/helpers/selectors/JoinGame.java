package helpers.selectors;

import static helpers.selectors.SelectorsHelper.metaId;

public interface JoinGame {
    String JOIN_GAME_MENU_ITEM = metaId("join-menu-option");
    String JOIN_GAME_BUTTON = metaId("join-game");
    String GAME_COE_INPUT =  metaId("game-code-input");
    String GAME_ID = metaId("game-id");
}
