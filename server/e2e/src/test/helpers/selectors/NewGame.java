package helpers.selectors;

import static helpers.selectors.SelectorsHelper.metaId;

public interface NewGame {
    String NEW_GAME_MENU_ITEM = metaId("new-game");
    String CREATE_BUTTON = metaId("create-game");
    String START_GAME_BUTTON =  metaId("start-game");
    String GAME_CODE = metaId("code");
    String GAME_ID = metaId("game-id");
}
